// Contador até 1000

let indice = 0;

while (indice <= 100) {
    console.log(indice);
    indice++;
}

for (let i = 0; i <= 100; i++) {
    console.log(indice);
}

// vegetais

let vegetais = ['Alface', 'tomate', 'batata', 'xuxu', 'pimenta', 'rabanete'];

vegetais.forEach(element => {
    console.log(element);
});